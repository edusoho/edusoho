parameters:
    biz_config:
        debug: "%kernel.debug%"
        db.options:
            dbname: edusoho-test
            user: tester
            password: tester
            host: "%database_host%"
            port: "%database_port%"
            driver: pdo_mysql
            charset: UTF8
            wrapperClass: Codeages\Biz\Framework\Dao\TestCaseConnection
        cache_directory: "%kernel.root_dir%/cache"
        log_directory: "%kernel.root_dir%/logs"