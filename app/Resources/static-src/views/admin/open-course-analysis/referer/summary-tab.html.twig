<div class="mbs">
  <div class= "btn-group" >
  	<a class="btn btn-default btn-sm {% if tab == 'list' %}btn-primary {% endif %}" href="{{path('admin_opencourse_analysis_referer_summary_list',{startTime:dateRange.startTime,endTime:dateRange.endTime})}}" role="button">明细</a>
  	<a class=" btn btn-default btn-sm {% if tab == 'summary' %}btn-primary {% endif %}" href="{{path('admin_opencourse_analysis_referer_summary',{startTime:dateRange.startTime,endTime:dateRange.endTime})}}" role="button">汇总</a>
  </div>
</div>