export const getTaskTitleJson = ($item, validator) => { 
  return {
    required: true,
    maxlength: 50,
    trim: true,
    open_live_course_title: true,
  }
}

export const getLiveTaskTitleJson = ($item, validator) => { 
  return {
    required: true,
    maxlength: 50,
    trim: true,
    open_live_course_title: true,
  }
}

