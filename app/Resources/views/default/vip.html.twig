
<!-- 会员 -->
{% set vipLevels = vip_level_list(config) %}
{% if vipLevels %}
  <section class="class-section {{config.background|default("")}} ">
    <div class="container">
      <div class="text-line">
        <h5>
          <span>{{config.title|default("vip"|trans)}}</span>
          <div class="line"></div>
        </h5>
        <div class="subtitle">{{ config.subTitle|default(config.defaultSubTitle) }}</div>
      </div>
      <div class="recommend-class-list row">
        {% for vipLevel in vipLevels %}
          <div class="col-md-3 prm plm">
            <div class="class-item" style="box-shadow:0px 0px 0px 0px rgb(0 0 0);">
              <a class="class-img">
                <img style="height: 88%" src="{% if vipLevel.picture %}{{ vipLevel.picture }}{% else %}{{ vipLevel.background }}{% endif %}" alt="">
              </a>
            </div>
          </div>
        {% endfor %}
      </div>
      <div class="section-more-btn">
        <a href="{{ path('vip') }}" class="btn btn-default btn-lg">
          {{ 'course.video.convert.audio.detail'|trans }} <i class="mrs-o es-icon es-icon-chevronright"></i>
        </a>
      </div>
    </div>
  </section>
{% endif %}
