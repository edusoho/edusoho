{% set category = categories[question.categoryId]|default(null) %}
<small class="text-muted">
  {{'course.question_manage.question.belong'|trans}}:
  {% if category|default(null) %}{{ category.name }}{% endif %}
  {% if not question.categoryId %}
    {{'site.data.empty'|trans}}
  {% endif %}
</small>
