{% extends 'courseset-manage/header.html.twig' %}

{% block js %}
{% endblock %}

{% block coursesetTitle %}
  {% if courseSet.status == 'published' %}
    <a class="cd-link-major" href="{{ path('course_show',{id: courseSet.defaultCourseId}) }}">{{ courseSet.title }}</a>
  {% else %}
    <a class="cd-link-major" href="{{ path('course_show', {id: courseSet.defaultCourseId, previewAs: 'member'}) }}">{{ courseSet.title }}</a>
  {% endif %}
{% endblock %}

{# 直播课程的label #}
{# {% block coursesetTag %}
  {% if courseSet.type == 'live' %}
    <span class="cd-tag cd-tag-green mr5">{{ 'my.teaching.course_set.live_label'|trans }}</span>
  {% endif %}
{% endblock %}
 #}

{% block btn %}
  <a class="cd-btn cd-btn-ghost-primary" target="_blank" href="{{ path('course_set_manage_base', {id: courseSet.id}) }}">{{ 'course.manage_btn'|trans }}</a>
{% endblock %}

{% block close %}
{% endblock %}