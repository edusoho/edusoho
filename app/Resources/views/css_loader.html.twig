{% for path in css() %}
  {% if path starts with 'http://' %}
    <link href="{{ path }}" rel="stylesheet" />
  {% else %}
    <link href="{{ asset('static-dist/' ~ path, 'webpack') }}" rel="stylesheet" />
  {% endif %}
{% endfor %}