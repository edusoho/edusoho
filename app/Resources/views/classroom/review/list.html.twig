{% extends 'classroom/content-layout.html.twig' %}

{% set nav = 'review' %}
{% block description %}{{ classroomDescription|default('')|plain_text(100) }}{% endblock %}
{% block main %}
	{% set goto = path('classroom_reviews',{id:classroom.id}) %}
	{% set canAccess = (member and 'headTeacher' in member.role) or 'ROLE_SUPER_ADMIN' in app.user.roles|default(null) or 'ROLE_ADMIN' in app.user.roles|default(null) %}

  {% include 'review/widget/list.html.twig' with { targetType:'classroom', targetId: classroom.id } %}

{% endblock %}
