

{% if log.data.id is defined %}

  {% set course = data('Course', {courseId: log.data.id}) %}

  {% set url = path('course_set_show', { id: log.data.id}) %}

  {{ 'log.action.template.course.update_course'|trans({'%url%': url, '%title%': course.title })|raw }}

{% else %}

  {{ log_trans(log.message, log.data) }}

{% endif %}
