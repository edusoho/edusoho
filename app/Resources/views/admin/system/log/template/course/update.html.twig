

{% if log.data.id is defined %}

  {% set courseSet = data('CourseSet', {id: log.data.id}) %}

  {% set url = path('course_set_show', { id: log.data.id}) %}

  {{ 'log.action.template.course.update'|trans({'%url%': url, '%title%': courseSet.title })|raw }}


{% else %}

  {{ log_trans(log.message, log.data) }}

{% endif %}
