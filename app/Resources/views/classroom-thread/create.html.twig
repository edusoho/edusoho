{% extends 'classroom/content-layout.html.twig' %}

{% set thread = thread|default(null) %}

{% block title %}{% if thread %}{{'classroom.thread.create.edit_btn'|trans}}{{ dict_text('threadType', type) }}{% else %}{{'classroom.thread.create.publish_btn'|trans}}{{ dict_text('threadType', type) }}{% endif %} - {{ parent() }}{% endblock %}

{% do load_script('thread/thread-form') %}
{% set nav = 'thread' %}
{% block main %}

    {{ render(controller('AppBundle:Thread:create', {request:app.request, target:{type:'classroom', id:classroom.id}, thread:thread, type:type})) }}

{% endblock %}
