{% extends "activity/content-layout.html.twig" %}

{% block body %}
	<div class="iframe-parent-content ph20 pv20">
    <div class="lesson-content-text-body">
      <div class="live-show-item">
        <p class="title">
          讨论主题
        </p>
        {{activity.content|raw}}
      </div>
      <div class="live-show-item">
        <a class="btn btn-primary" href="{{path('course_thread_create', {courseId: activity.fromCourseId, type: 'discussion'})}}" target="_blank">前往课程讨论区</a>
      </div>
    </div>
	</div>
{% endblock %}