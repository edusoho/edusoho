{% extends 'activity/activity-form-layout.html.twig' %}
{% do script(['web/activity-manage/text/index.js'], 300) %}

{% block activity_content %}
  <div class="form-group">
    <div class="col-sm-2 control-label">
      <label for="text-content-field">{{ '内容'|trans }}</label>
    </div>
    <div class="col-sm-10">
      <textarea class="form-control type-hidden" id="text-content-field" name="content"data-image-upload-url="{{ path('editor_upload', {token:upload_token('course')}) }}" data-flash-upload-url="{{ path('editor_upload', {token:upload_token('course', 'flash')}) }}" data-image-download-url="{{ path('editor_download', {token:upload_token('course')}) }}" >{{ activity.content|default('') }}</textarea>
    </div>
  </div>
{% endblock %}

{% block activity_finish %}
  <div class=" form-group">
    <div class="col-sm-2 control-label">
      <label for="condition-select">{{ '完成条件'|trans }}</label>
    </div>
    <div class="col-sm-4">
      <select class="form-control" id="condition-select" name="finishType">
        <option value="time" {% if text.finishType|default('end') == 'time' %}selected{% endif %}>至少观看X分钟</option>
      </select>
    </div>
  </div>
  <div class=" form-group" id="condition-group">
    <div class="col-sm-2 control-label">
      <label for="finishDetail">{{ '至少观看'|trans }}</label>
    </div>
    <div class="col-sm-4">
      <input id="finishDetail" class="form-control" type="text" name="finishDetail" value="{{ text.finishDetail|default(1) }}">
    </div>
    <div class="col-sm-4">
      <p class="form-control-static">分钟</p>
    </div>
  </div>
{% endblock %}