{% set article = data('Article', {id:data.post.targetId})%}
<a href="{{ user_show_path({id:data.user.id}) }}" target="_blank">{{ data.user.nickname }}</a>
{{'article.reply_tips'|trans({'%jumpUrl%':path('article_post_jump', {articleId:data.post.targetId, postId:data.id}), '%title%':article.title|default()})|raw}}
<blockquote>
  {{ data.content }}
</blockquote>
