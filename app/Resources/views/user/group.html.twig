{% extends 'user/layout.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% set pageNav = 'group' %}

{% block main %}
  {% if adminGroups %}
    <div class="es-piece">
      <div class="piece-header">
        {{'管理小组'|trans}}
      </div>
      {% include 'my/learning/group/groups-ul.html.twig' with {'groups':adminGroups}%}
    </div>
    <hr>
  {% endif %}
  <div class="es-piece">
    <div class="piece-header">
      {{'加入小组'|trans}}
    </div>
    {% include 'my/learning/group/groups-ul.html.twig' with {'groups':groups} %}
  </div>
  {{ web_macro.paginator(paginator) }}
{% endblock %}