{% extends 'layout.html.twig' %}

{% block title %}{{'课程管理'|trans}} - {{ parent() }}{% endblock %}
{% set use_webapck_loader = true %}

{% block content %}
{{ render(controller('AppBundle:CourseSetManage:header', {courseSet:courseSet, course: course|default(null), manage: true})) }}

<div class="row">
  <div class="col-md-3">
    {{ render(controller('AppBundle:CourseSetManage:sidebar', {courseSetId:courseSet.id, sideNav: side_nav|default(null)})) }}
  </div>
  <div class="col-md-9">
    {% block main %}{% endblock %}
  </div>
</div>
{% endblock %}