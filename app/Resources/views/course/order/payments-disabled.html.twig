{% extends 'old-bootstrap-modal-layout.html.twig' %}

{% block title %}{% if course.price > 0 %}{{'course.btn.buy'|trans}}{% else %}{{'course.btn.join'|trans}}{% endif %}{% endblock %}

{% block body %}
	<div class="alert alert-info">{{ setting('payment.disabled_message')|default('buy.payments_disabled_tips') | trans | raw }}</div>
{% endblock %}
