{#{% set canAccess = (member and 'teacher' in member.role) or 'ROLE_SUPER_ADMIN' in app.user.roles|default('') or 'ROLE_ADMIN' in app.user.roles|default('') %}#}
{% set canAccess =  member.role|default('') == 'teacher' or has_permission('admin_course_content_manage') %}
{% if not member|default(null) %}
  <!-- 1、加入前 -->
  {% include 'course/part/header-for-guest.html.twig' %}
{% else %}

  {% if course.learnMode == 'freeMode' %}
    <!-- 2、加入后：自由式学习 -->
    {% include 'course/part/free-mode-header-for-member.html.twig' %}
  {% elseif course.learnMode == 'lockMode' %}
    <!-- 3、加入后：任务式学习 -->
    {% include 'course/part/lock-mode-header-for-member.html.twig' %}
  {% endif %}
{% endif %}

