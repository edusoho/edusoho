{% if member.deadline < timestamp() and member.deadline != 0 %}
  {% include 'course/member/normal-course-expired-member.html.twig' %}
{% elseif course.expiryMode == 'date' and course.expiryStartDate > timestamp() %}
  {% include 'course/member/normal-course-expired-course.html.twig' %}
{% else %}
  {{ slot('course.expired.message', {course: course,member: member}) }}
{% endif %}