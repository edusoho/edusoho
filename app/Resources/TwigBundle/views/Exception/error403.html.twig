{% extends 'TwigBundle:Exception:layout.html.twig' %}
{% block title %}访问被拒绝 - {{ parent() }}{% endblock %}

{% block body %}
  <div class="container">
    <div class="panel panel-default error-panel">
      <div class="panel-heading">
        <span class="error-title">403</span>访问被拒绝
      </div>
      <div class="panel-body">
        <div class="well">
          <p>
            {% if exception.message %}
              {{ exception.message }}
            {% else %}
               你没有权限访问该页面！
            {% endif %}
          </p>
          <p><a href="{{ path('homepage', {ref:403}) }}">返回首页 ></a></p>
          <span class="error-icon hidden-xs"></span>
        </div>
      </div>
    </div>
  </div>
{% endblock %}