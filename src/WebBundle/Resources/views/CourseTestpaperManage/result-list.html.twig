{% extends 'WebBundle:CourseSetManage:layout.html.twig' %}
{% import 'TopxiaWebBundle::macro.html.twig' as macro %}

{% block title %}{{'试卷批阅列表'|trans}} - {{ parent() }}{% endblock %}

{% set side_nav = 'testpaperCheck' %}
{% set parentId = parentId | default(null)%}
{% set script_controller = 'course-manage/testpaper-result-list' %}

{% block main %}

<div class="panel panel-default panel-col">

  <div class="panel-heading">
    {{ testpaper.name }}
  </div>
  <div class="panel-body">

    {% if isTeacher is defined and not isTeacher %}
      <span class="text-danger">{{'当前用户不是本课程教师，没有批改试卷的权限。'|trans}}</span>
    {% endif %}

    {{ render(controller('WebBundle:TestpaperManage:resultList',{request:app.request,testpaperId:testpaper.id})) }}

  </div>
</div>

{% endblock %}