admin_course_next_round:
    pattern: /course/{id}/nextRound
    defaults: { _controller: CustomAdminBundle:RoundCourse:nextRound }

admin_course_rounding:
    pattern: /course/{id}/rounding
    defaults: { _controller: CustomAdminBundle:RoundCourse:rounding }

admin_classroom_create:
    pattern:  /classroom/create
    defaults: { _controller: CustomAdminBundle:ClassroomAdmin:addClassroom }

admin_user_edit:
    pattern:  /user/{id}/edit
    defaults: { _controller: CustomAdminBundle:User:edit }