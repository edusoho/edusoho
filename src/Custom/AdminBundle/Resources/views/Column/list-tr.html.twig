<tr id="column-tr-{{ column.id }}">
  <td>{{ column.id }}</td>
  <td>{{ column.name }}</td>
  <td>{{column.description|default("")}}</td>
  <td>{{ column.createdTime|date('Y-m-d H:i') }}</td>
  <td>
         <div class="btn-group">
                   <a href="#modal" data-toggle="modal" data-url="{{ path('admin_column_update', {id:column.id}) }}" data-url="" class="btn btn-default btn-sm">编辑</a>
     	    <a href="#" type="button" class="btn btn-default btn-sm dropdown-toggle" data-toggle="dropdown">
       	          <span class="caret"></span>
                 </a>
	      <ul class="dropdown-menu">
              <li><a href="#modal" data-toggle="modal" data-url="{{ path('admin_column_avatar', {id:column.id}) }}">设置专栏图标</a></li>
	      </ul>
        </div>
   </td>
 </tr>
    {#      </div>
    <button class="btn btn-default btn-sm" data-url="{{ path('admin_tag_update', {id:tag.id}) }}" data-toggle="modal" data-target="#modal">编辑</button>
  </td>
</tr> #}