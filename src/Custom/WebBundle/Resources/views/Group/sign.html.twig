<div class="panel panel-default">
  <div class="panel-body">
    <div class="media" id="group-sign" data-signurl="{{path('group_sign',{groupId:groupinfo.id})}}">
     
        <a class="btn btn-info btn-lg pull-left" style="width:100px;font-size:25px;" data-role="sign" > 签 到 </a>
{# <a class="btn btn-info btn-lg pull-left" style="width:100px;font-size:12px;text-align:left;">已签到 
  <br>连续41天</a> #}
   
    <div class="media-body pll">
      <h4 class="media-heading pts">{{ 'now' | date('m') }}月{{ 'now' | date('d') }}号</h4>
      漏签<span style="color:red;">10</span>天
    </div>
  </div>

  </div>
</div>
 

<div class="class-sign class-sign-finished row">
  <div class="date-col col-md-6">
    <div class="date-area">
      <div class="day">{{ 'now' | date('d') }}</div>
  
      <div class="yearMonth">{{ 'now'|date('Y.m') }}</div>
    </div>
  </div>

  <div class="action-col  col-md-6">
  <div id="class-sign"  data-records="{{ path('signed_records', {'groupId':groupinfo.id})}}" data-signurl="{{path('group_sign',{groupId:groupinfo.id})}}" >
  <div  class="sign-area" {% if not isSignedToday %} data-role="sign" {% else %} data-role="signed" onclick="return false;" {% endif %} >
 
    {% if not isSignedToday %}
      <span class="sign-text">签到</span>
    {% else %}
      <span class="sign-text">已签</span>
    {% endif %}
  </div>
  
  <div class="sign_main" style="display:none">
    <div class="">
      <div class="calendar_title_month clearfix">
        <span class="pull-left glyphicon glyphicon-chevron-left title-margin" data-role="previous"></span>
        <span class="pull-right glyphicon glyphicon-chevron-right title-margin disabled-next" data-role="next"></span>
        <div class="text-center title-month title-margin" data-time="{{ "now"|date('Y/m') }}" id="title-month">{{ "now"|date('Y年m月') }}
        </div>
      </div>
      
      <table class="sign_succ_table ">
        <thead align="center">
          <tr class="sign_succ_canlerdar_head">
            <td>日</td><td>一</td><td>二</td><td>三</td><td>四</td><td>五</td><td>六</td>
          </tr>
        </thead>
        <tbody align="center" class="sign_succ_canlerdar_days j_canlerdar_days">

        </tbody>
      </table>
    </div>
    <div class="sign_tip_boards">
      <div class="mlm mtm">
        <p class=" text-muted">签到排名：今日本班第<span class="today-rank"></span>个签到</p>
      </div>
      <div class="mlm">
        <p class=" text-muted">本班签到人数：<span class="signed-number"></span></p>
      </div>
      <div class="mlm">
        <p class=" text-muted">连续签到：<span class="keep-days"></span>天</p>
      </div>
    </div>
  </div>
</div>
  </div>
</div>
 