<div class="nav-btn-tab">
  <ul class="nav nav-tabs" role="tablist">
    <li role="presentation" class="{% if nav == 'introduction' %}active{% endif %}"><a  href="{{ path('classroom_introductions', {id:classroom.id}) }}">介绍</a></li>

    <li role="presentation" class="{% if nav == 'course' %}active{% endif %}"><a href="{{ path('classroom_courses', {classroomId:classroom.id}) }}">课程 <small class="text-muted">( {{classroom.courseNum}} )</small></a></li>

  {% if not classroom.private or (classroom.private and member) %}
    <li role="presentation" class="{% if nav == 'review' %}active{% endif %}"><a href="{{ path('classroom_reviews', {id:classroom.id}) }}">评价 <small class="text-muted">( {{classroom.ratingNum}} )</small></a></li>

    <li role="presentation" class="{% if nav == 'thread' %}active{% endif %}"><a href="{{ path('classroom_threads', {classroomId:classroom.id}) }}">话题 <small class="text-muted">( {{classroom.threadNum}} )</small></a></li>

    <li role="presentation" class="{% if nav == 'note' %}active{% endif %}"><a  href="{{ path('classroom_course_notes_list', {classroomId:classroom.id}) }}">笔记 <small class="text-muted">( {{ classroom.noteNum }} )</small></a></li>
  {% endif %}

    <li role="presentation" class="{% if nav == 'teachers' %}active{% endif %}"><a href="{{ path('classroom_teachers', {classroomId:classroom.id}) }}">师资</a></li>

  </ul>
  
  <div class="btnbar hidden-xs">
    <a href="#modal" data-toggle="modal" data-url="{{ path('announcement_add', {targetType:'classroom', targetId:classroom.id}) }}" class="btn btn-link">
      <i class="es-icon es-icon-anonymous-iconfont"></i>公告
    </a>
  </div>
</div>