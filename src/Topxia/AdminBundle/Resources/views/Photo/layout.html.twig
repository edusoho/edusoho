{% extends 'TopxiaAdminBundle::layout.html.twig' %}

{% set nav = 'photo' %}

{% block content %}
  <div class="col-md-2">
    {% block sidebar %}
      <div class="list-group">
        <a href="{{ path('admin_photo') }}" class="list-group-item {% if menu == 'photo' %}active{% endif %}">相册管理</a>
      </div>
    {% endblock %}
  </div>
  <div class="col-md-10">
      {% block main %}{% endblock %}
  </div>
{% endblock %}