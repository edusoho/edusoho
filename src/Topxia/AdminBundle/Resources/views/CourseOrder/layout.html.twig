{% extends 'TopxiaAdminBundle::layout.html.twig' %}

{% set nav = 'trade' %}

{% block content %}
  <div class="col-lg-2">

    {% block sidebar %}
      <div class="list-group">
        <a href="{{ url('admin_course_order_manage') }}" class="list-group-item {% if menu == 'course-order' %}active{% endif %}">
          课程订单管理 <span class="glyphicon glyphicon-chevron-right"></span>
        </a>
      </div>
    {% endblock %}
  </div>
  <div class="col-lg-10">
      {% block main %}{% endblock %}
  </div>
{% endblock %}