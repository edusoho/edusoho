<div class="btn-group">

	<a href=" {{ url('admin_invite_coupon_list', {filter: 'invited'}) }}" type="button" class="btn btn-default btn-sm {% if filter == 'invited'%}btn-primary{% endif %}">注册奖励</a>


	<a href=" {{ url('admin_invite_coupon_list', {filter: 'invite'}) }}" type="button" class="btn btn-default btn-sm {% if filter == 'invite' %}btn-primary{% endif %}">邀请奖励</a>
</div>