{% extends 'TopxiaAdminBundle::layout.html.twig' %}

{% block main %}
  <ul class="nav nav-pills mbl small">
    <li {% if currentPage == 'admin_block_visual_edit' %}class="active"{% endif %}>
      <a href="{{ path('admin_block_visual_edit',{blockTemplateId:block.blockTemplateId}) }}">编辑</a>

    </li>
    <li {% if currentPage == 'admin_block_visual_edit_history' %}class="active"{% endif %}>
      <a href="{{ path('admin_block_visual_edit_history',{blockTemplateId: block.blockTemplateId}) }}">历史记录</a>
    </li>
  </ul>
  {% block blockVisual %} {% endblock %}
{% endblock %}