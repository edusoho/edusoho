{% extends 'TopxiaAdminBundle:System:layout.html.twig' %}

{% block title %}登录注册 - {{ parent() }}{% endblock %}

{% set menu = 'auth' %}

{% block main %}

<div class="page-header"><h1>登录注册设置</h1></div>

{{ web_macro.flash_messages() }}

<form class="form-horizontal" method="post" novalidate>
  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.register_mode, '新用户注册') }}</div>
    <div class="controls col-lg-8 radios">{{ form_widget(form.register_mode) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.agreement, '用户协议') }}</div>
    <div class="controls col-lg-8">{{ form_widget(form.agreement) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">
      {{ form_label(form.email_activation_mode, '新用户邮件激活') }}
    </div>
    <div class="controls col-lg-8 radios">{{ form_widget(form.email_activation_mode) }}
      <div class="help-block">
        <ul>
          <li>必须激活：用户必须通过邮件激活，才能登录。</li>
          <li>推荐激活：用户注册注册成功后，会发送激活邮件，但不强制激活。</li>
          <li>关闭：用户注册时，不会出现邮件激活的步骤。</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.email_activation_title, '新用户激活邮件标题') }}</div>
    <div class="controls col-lg-8">{{ form_widget(form.email_activation_title) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.email_activation_body, '新用户激活邮件内容') }}</div>
    <div class="controls col-lg-8">{{ form_widget(form.email_activation_body) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.welcome_methods, '发送欢迎信息') }}</div>
    <div class="controls col-lg-8 checkboxs">{{ form_widget(form.welcome_methods) }}
        <div class="help-block">
          新用户邮件激活开启时，电子邮件的发送欢迎信息方式无效。
        </div>
    </div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.welcome_title, '欢迎信息标题') }}</div>
    <div class="controls col-lg-8">{{ form_widget(form.welcome_title) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label">{{ form_label(form.welcome_body, '欢迎信息内容') }}</div>
    <div class="controls col-lg-8">{{ form_widget(form.welcome_body) }}</div>
  </div>

  <div class="row control-group">
    <div class="col-lg-3 control-label"></div>
    <div class="controls col-lg-8">
      <button type="submit" class="btn btn-primary">提交</button>  
      {{ form_rest(form) }}
    </div>
  </div>
</form>

{% endblock %}