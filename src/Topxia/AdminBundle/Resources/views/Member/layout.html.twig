{% extends 'TopxiaAdminBundle:User:layout.html.twig' %}

{% block title%}会员管理 -{{ parent() }}{% endblock %}

{% set menu = 'member' %}

{% set submenu = 'memberall' %}

{% block main %}


    <div class="page-header clearfix">
      <button class="btn btn-info btn-sm pull-right" id="add-navigation-btn" data-toggle="modal" data-target="#modal" data-url="{{path('admin_member_create')}}">添加新会员</button>

      <h1 class="pull-left">会员管理</h1>
    </div>

    {% block nav-tabs %}
      <ul class="nav nav-tabs">
        <li {% if submenu == 'memberall' %} class="active" {% endif %} >
          <a  href="{{ path('admin_member_all') }}">全部会员</a>
        </li>
        <li {% if submenu == 'memberexpire' %} class="active" {% endif %} >
          <a  href="{{ path('admin_member_expire') }}">即将到期的会员</a>
        </li>
         <li {% if submenu == 'memberjustexpire' %} class="active" {% endif %} >
          <a  href="{{ path('admin_member_justexpire') }}">刚刚到期的会员</a>
        </li>
      </ul>
    {% endblock %}
    {% block search %}{% endblock %}
    {% block table %}{% endblock %}
