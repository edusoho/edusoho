{% extends 'TopxiaAdminBundle:Operation:card.layout.html.twig' %}
{% block title %}学习卡 - {{ parent() }}{% endblock %}

{% set submenu = 'money_card_homepage' %} 
{% set script_controller = 'moneycardbundle/controller/batch-list' %}

{% block maincontent %}
  		
  <div class="page-header clearfix">
     <button class="btn btn-info btn-sm pull-right" id="add-navigation-btn" data-toggle="modal" data-target="#modal" data-url="{{path('admin_money_card_create')}}">生成学习卡</button>
    <h1 class="pull-left">学习卡批次管理</h1>
  </div>

    <form id="money-card-batch-search-form" class="form-inline well well-sm" action="" method="get" novalidate>

        <div class="form-group">
          <input type="text" id="batchName" name="batchName" class="form-control" value="{{ app.request.query.get('batchName') }}" placeholder="学习卡批次名称">
        </div>

        <button class="btn btn-primary">搜索</button>

        <style type="text/css">
            .popover{
              z-index: 9999;
            }
        </style>
        <span class="glyphicon glyphicon-question-sign text-muted pull-right" id="article-property-tips" data-toggle="tooltip" data-placement="bottom" href="javascript:" title="" data-original-title=""></span> 
         <div id="article-property-tips-html" style="display:none;">
            <div style="width:250px;">
              <span style="color:blue;">学习卡说明</span><br>
              1、学习卡一次最多生成1000张，每一张学习卡都会对应一对卡号与密码；<br>
              2、每一张学习卡只能使用一次；<br>
              3、学习卡可以用来充值网校虚拟币，可以用来购买课程和其他网校虚拟币支付行为。<br>
              <span style="color:blue;">学习卡建议</span><br>
              1、建议网校采取线下推广策略；<br>
              2、线下推广时可采取部分优惠；<br>
            </div>
        </div>
    </form>


    <table id="money-card-batch-table" class="table table-striped table-hover" data-search-form="#money-card-batch-search-form">
      <thead>
        <tr>
          <th>批次id</th>
          <th>批次名称</th>
          <th>额度(虚拟币)</th>
          <th>生成张数</th>
          <th>已使用</th>
          <th>有效期</th>
          <th>生成人</th>
          <th>状态</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
      {% if batchs %}
        {% for batch in batchs %}
            {% include 'TopxiaAdminBundle:Operation:batch-table-tr.html.twig' with {batch:batch} %}
        {% endfor %}
      </tbody>
    </table>
      {% else %}
      </tbody>
    </table>
    <div class='empty'>数据库君表示自己非常不给力，啥都没找到~~</div>
      {% endif %}
    {{ web_macro.paginator(paginator) }}

{% endblock %}
