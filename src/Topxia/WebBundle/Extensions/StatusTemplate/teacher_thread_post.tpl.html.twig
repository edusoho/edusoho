{% set props = status.properties %}
{% set user = data('User',{'userId':props.thread.userId}) %}

{% if mode == 'simple' %}
  <a class="link-dark" href="{{ path('user_show', {id:user.id}) }}">{{ user.nickname }}</a>的问题"{{ props.thread.title|sub_text(15)}}"已被教师回复
{% elseif mode == 'full' %}

{% endif %}