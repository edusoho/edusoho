{% extends 'TopxiaWebBundle:Classroom:dashboard-layout.html.twig' %}

{% set post = post|default(null) %}

{% block title %}{% if post %}编辑帖子{% else %}回复帖子{% endif %} - {{ parent() }}{% endblock %}

{% set script_controller = 'thread/thread-post-form' %}

{% block dashboard_main %}

<ul class="breadcrumb">
  <li><a href="{{ path('threads', {targetType:targetType,id:targetId}) }}">话题</a></li>
  <li><a href="{{ path('thread_show', {targetType:targetType,targetId:thread.targetId, id:thread.id}) }}">{{ thread.title|plain_text(10) }}</a></li>
  <li class="active">编辑帖子</li>
</ul>

<form id="post-thread-form" method="post"
  {% if post %}
    action="{{ path('thread_post_edit', {targetType:targetType,targetId:targetId, threadId:post.threadId, id:post.id}) }}"
  {% else %}
    action="{{ path('thread_post', {targetType:targetType,targetId:targetId, id:post.id}) }}"
  {% endif %}
  >
  <div class="form-group">
    <div class="controls">
      {{ form_widget(form.content, {attr:{class:'form-control', rows:15, 'data-display':'内容'}}) }}
    </div>
  </div>
  <div class="form-group">
    <div class="controls clearfix">
      {{ form_rest(form) }}
      <button type="submit" class="btn btn-primary pull-right">发表</button>
      <a href="{{ path('threads', {targetType:targetType,id:targetId}) }}" class="btn btn-link pull-right">取消</a>
    </div>
  </div>

  <input type="hidden" name="_csrf_token" value="{{ csrf_token('site') }}">
</form>

{% endblock %}
