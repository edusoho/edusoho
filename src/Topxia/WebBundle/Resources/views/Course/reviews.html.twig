{% extends 'TopxiaWebBundle:Course:normal-layout.html.twig' %}

{% set thread = thread|default(null) %}

{% block title %}{{ parent() }}{% endblock %}

{% set tab = 'review' %}

{% block course_main %}

	{% set goto = path('course_review_list',{id:course.id}) %}
	{% set canAccess = (member and 'teacher' in member.role) or 'ROLE_SUPER_ADMIN' in app.user.roles|default('') or 'ROLE_ADMIN' in app.user.roles|default('') %}
	
  {% include 'TopxiaWebBundle:Review:Widget/list.html.twig' %}

{% endblock %}