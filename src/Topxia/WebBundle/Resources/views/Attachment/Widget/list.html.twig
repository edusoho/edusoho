{% set attachments = data('AttachmentList',{targetType: targetType, targetId: targetId}) %}
{% include 'TopxiaWebBundle:Attachment:list.html.twig' %}

{# {{ render(controller('TopxiaWebBundle:Attachment:list', {targetType: targetType, targetId: targetId })) }} #}