<tr id="{{ category.id }}" class="catgory-tr">
  <td>
  <span class="number">{{category.seq}}</span>
  </td>
  <td>
    {{ category.name }}
  </td>
  <td>
	<button type="button" data-id="{{category.id}}" class="btn btn-default btn-sm delete-category" data-url="{{ path('course_manage_quiz_question_category_delete', {courseId:course.id,categoryId :category.id}) }}" tabindex="5"><i class="glyphicon glyphicon-trash"></i> 删除</button>
    <button class="btn btn-default btn-sm "  data-url="{{ path('course_manage_quiz_question_category_edit', {courseId:course.id,categoryId:category.id}) }}" data-toggle="modal" data-target="#modal"><i class="glyphicon glyphicon-edit"></i> 编辑</button>
  </td>
</tr>