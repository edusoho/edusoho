{% extends 'TopxiaWebBundle::layout.html.twig' %}

{% block content %}
  <div class="question-set homework-set" style="background:white;">

    <div class="question-set-items">
      {% for item in items %}
        {% set question = questions[item.questionId]|default(null) %}
        {% include 'TopxiaWebBundle:Question:question-set-item.html.twig' %}
      {% endfor %}
    </div>

  </div>
{% endblock %}