{% extends 'TopxiaWebBundle:CourseManage:layout.html.twig' %}

{% block title %}{{title}} - {{ parent() }}{% endblock %}

{% set side_nav = type %}
{% block main %}
<style>
  .panel-locked{
    margin:80px 50px;
  }
  .panel-locked p{
    text-align: center;
    line-height:35px;
  }
  .panel-margin{
    margin-top:50px;
  }
  b{}
</style>
<div class="panel panel-default panel-col">
  <div class="panel-heading">{{title}}</div>
  <div class="panel-body">
    <div class="panel-locked">
      <h5><p>本课程的内容跟原课程《考试、作业、练习功能体验课程》同步实时更新。</b></p></h5>
      <h5><p class="panel-margin"><a href="{% if type == 'question' or type == 'testpaper' %}{{path(url,{courseId:course.id})}}{% else %}{{path(url,{id:course.id})}}{% endif %}" class="btn btn-success">编辑</a>&nbsp;&nbsp;开始编辑课程内容，但是一旦保存后，本课程将<font color="#f3b760">不再同步实时更新</font>，全部内容需要您手动编辑。</h5>
    </div>
  </div>
</div>

{% endblock %}



