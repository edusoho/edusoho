{% set settings = setting('login_bind', []) %}

{% for type, client in clients %}
  {% if settings[type ~ '_enabled']|default(false) and type != 'weixinmob'%}
    <a href="{{ path('login_bind', {type: type, _target_path:targetPath|default('')}) }}" class="social-icon icon-{{ client.icon_class }}">
      {% if client.icon_class|default(null) %}
        <i class="es-icon es-icon-{{ client.icon_class }}"></i>
      {% endif %}
    </a>
  {% endif %}
{% endfor %}

{#
{% for type, client in clients %}
  {% if settings[type ~ '_enabled']|default(false) and type != 'weixinmob'%}
    <a href="{{ path('login_bind', {type: type, _target_path:targetPath|default('')}) }}" class="social-login-btn hidden">
      {% if client.icon_class|default(null) %}
        <i class="{{ client.icon_class }}"></i>
      {% elseif client.icon_img|default(null) %}
        <img src="{{ asset(client.icon_img) }}" class="social-img">
      {% endif %}
      {% if displayName %} {% if type == 'weixinweb'%}微信登录{% else %}{{ client.name }} {% endif %}{% endif %}
    </a>
  {% endif %}
{% endfor %}
#}
