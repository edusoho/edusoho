{% for path in script()  %}
  {% if path starts with 'http://' %}
    <script src="{{ path ~ '?' ~ asset_version('')}}"></script>
  {% else %}
    <script src="{{ asset('build/' ~ path, 'webpack') }}"></script>
  {% endif %}
{% endfor %}


