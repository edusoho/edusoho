{% if materials and ids|length > 1 %}
  <div class="controls mll mtl">
    <ul>
      {% for fileId,material in materials %}
        {% if  material|length > 0 and loop.index <= 3 %}
          <li>文件：{{ files[fileId].filename }} 被引用</li>
        {% endif %}
      {% endfor %}
      {% if materials|length > 3 %}
        ......
      {% endif %}
    </ul>
  </div>
{% endif %}