{% set userbind = data('UserBind',{count:1}) %}
{% if not userbind['isBind']|default(1) and not app.request.cookies.get('close_weixin_alert')%}
  <div class="alert alert-warning alert-dismissible weixin-alert" role="alert">
    您是微信账号登录，如果已有账号，请<a href="{{ path('login_bind_weixin') }}"><font color=blue>绑定</font></a>，以免丢失学习记录
    <span class="close" data-dismiss="alert">x</span>
  </div>
{% endif %}