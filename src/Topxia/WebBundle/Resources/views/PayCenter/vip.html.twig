 <div class="order-detail-bg  checkout" style="border:none">
   <div class="order-detail  clearfix">
     <div class="order-img hidden-xs">
        <a href="{{ path('vip_course_explore', {levelId:level.id}) }}">
          <img class="img-responsive" src="{{ level.picture|default(asset('assets/img/default/vip-default.png')) }}"alt="收费">
        </a>
        {# {% if is_plugin_installed('ChargeCoin') and setting('coin.charge_coin_enabled') and order.targetType == "coin" %}
        <a href="javascript:;">
         <img class="img-responsive" src="{{asset('assets/img/default/classroom-default-213-142.png') }}"alt="收费"></a>
       {% endif %} #}
     </div>
     <div class="order-info">
       <p>订单名称：{{ order.title }}</p>
       <p>订单号：{{order.sn}}</p>
       <p>
         订单价格：
         <span class="pay-rmb">￥{{order.amount}}</span>
       </p>
     </div>
   </div>
 </div>