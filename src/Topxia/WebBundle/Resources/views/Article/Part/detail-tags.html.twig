<div class="article-tags">
  {% if tagNames %}{{'标签：'|trans}}
    {% for tagName in tagNames %}
    <a class="btn-tag" href="{{path('article_tag_show',{name:tagName})}}">{{tagName}}</a>
    {% endfor %}
  {% endif %}
</div>