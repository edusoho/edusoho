{% import "TopxiaWebBundle::macro.html.twig" as web_macro %}
<div class="toolbar toolbar-open">
	<form class="form-inline mark-from" action="{{path('course_lesson_question_search',{courseId:course.id,lessonId:lesson.id})}}" method="post" novalidate>
		<input type="hidden" name="_csrf_token" value="{{ csrf_token('site') }}">	
		<div class="form-group">
			<select class="form-control" name="target" style="width:120px;">
				{{ select_options(targetChoices, app.request.query.get('target'))}}
			</select>
		</div>

		<div class="form-group">
			<input type="text" id="keyword" name="keyword" class="form-control" value="{{ app.request.query.get('keyword') }}" placeholder="关键词"></div>

		<button class="btn btn-primary btn-md">搜索</button>
	</form>

	{#<table class="table table-striped table-hover" id="quiz-table">
		<thead>
			<tr>
				<th width="10%"></th>
				<th width="50%">题干</th>
				<th>类型</th>
				<th>难度</th>
				<th>操作</th>
			</tr>
		</thead>
		<tbody class="question-tr">{% include 'TopxiaWebBundle:Marker:question-tr.html.twig' %}</tbody>
	</table>#}
	{% include 'TopxiaWebBundle:Marker:question-tr.html.twig' %}
</div>
<script>app.load('marker/question')</script>
