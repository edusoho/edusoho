{% import "TopxiaWebBundle::macro.html.twig" as web_macro %}
  {% for question in questions %}
  <li class=" item-lesson {% if question.exist|default(false) %}disdragg {% else %} drag {% endif %}" id="" question-id="{{question.id}}" data-role="scale-blue-item" data-url="{{ path('course_manage_question_preview', {id:question.id, courseId:course.id}) }}">
    <span class="sqe-number"><i class="question-remove es-icon es-icon-icon_close_circle" data-role="question-remove"></i><span data-role="sqe-number">1</span>、</span>
    <span class="title">
      <i class="icon-drag glyphicon glyphicon-move"></i>
      <span class="color-primary text-xs" data-role="question-type">[{{ dict('questionType')[question.type] }}]</span>
    </span>
    {{question.stem|fill_question_stem_text|plain_text(12) }}
    <a class="btn-preview js-question-preview" href="javascript:;" data-toggle="modal" data-target="#modal" data-url="{{ path('course_manage_question_preview', {id:question.id, courseId:course.id}) }}"><i class="es-icon es-icon-visibility" ></i></a>
  </li>


  {#<li class="row item-lesson {% if question.exist|default(false) %}disdrag {% else %} drag {% endif %}" id="" question-id="{{question.id}}">
    <div class="col-md-6 col-sm-10 title">
      <div class="before">
        <span class="icon-drag glyphicon glyphicon-move mrm "></span>
        <span class="number"><span class="num">1</span>.</span>
        {% if question.includeImg %}<span class="glyphicon glyphicon-picture"></span>{% endif %}
        {{question.stem|fill_question_stem_text|plain_text(12) }}
      </div>
      <i class="icon-close es-icon es-icon-icon_close_circle"></i>
    </div>
    <div class="col-md-3 content">{{ dict('questionType')[question.type] }}</div>
    <div class="col-md-1 content">{{ dict('difficulty')[question.difficulty] }}</div>
    <div class="col-xs-2 content not-add"><a href="javascript:;" class="marker-preview" data-url="{{ path('course_manage_question_preview', {id:question.id, courseId:course.id}) }}">{{'预览'|trans}}</a></div>
    <div class="col-xs-2 content have-add">{{'已添加'|trans}}</div>
  </li>#}





  {% else %}
    <div class="empty">{{'一道题都没有'|trans}}</div>
  {% endfor %}
  {% if paginator.lastPage > 1 %}
  <li class="more-questions" data-last-page="{{ paginator.lastPage }}" data-current-page="{{ paginator.currentPage }}" data-url='/course/{{ course.id }}/lesson/{{ lesson.id }}/question_marker/search?page='><span class="loading"><img src="{{ asset('assets/img/default/loading.gif') }} ">正在加载...</span><a class="js-more-questions" href="javascript:;" >更多题目<i class="es-icon es-icon-chevronright
arrow-back
"></i></a></li>
  {% endif %}
<!-- {{ web_macro.paginator(paginator) }}  -->


