<div>
  <div class="text-track-title">
    {% set token = uploader_token('text-track', course.id, 'private') %}
    <div class="row">
      <div class="col-md-3 caption-uploader js-select-file" id="uploader"
      data-init-url="{{ url('uploader_init', {token:token}) }}"
      data-finish-url="{{ url('uploader_finished', {token:token}) }}"
      >
      </div>
      <div class="col-md-9 caption-notify">
        <i class="es-icon es-icon-xinxi remask-icon color-warning  mll"></i>
        仅支持srt字幕格式，最多上传4个字幕
      </div>
    </div>
  </div>
  <div class="text-track-list">
    <form class="form-horizontal js-texttrack-select mrl" action="{{path('course_lesson_question_marker_search',{courseId:course.id,lessonId:lesson.id})}}" method="post" novalidate>
      <input type="hidden" name="_csrf_token" value="{{ csrf_token('site') }}">
      <div class="form-group text-track-select-form-group">
        <div class="mtl ptm phm">
          <select class="form-control js-track-select hidden" id="track-select" name="target" >
            无字幕文件
            <option value="http://esc1a1b7gz-pub.upcdn.edusoho.net/captions_eng_srt">captions_eng_srt</option>
            <option value="http://esc1a1b7gz-pub.upcdn.edusoho.net/captions_chs_srt">captions_chs_srt</option>
            <option value="http://esc1a1b7gz-pub.upcdn.edusoho.net/captions_eng_chs_srt">captions_eng_chs_srt</option>
          </select>
        </div>
      </div>
    </form>
    <div class="text-track-overview mrl">
    </div>
  </div>
</div>
<script type="text/javascript">app.load('marker/text-track')</script>