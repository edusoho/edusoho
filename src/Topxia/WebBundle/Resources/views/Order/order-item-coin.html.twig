<div class="row order-item">
  <div class="order-item-title">
    <i class="order-item-title-icon">
      <img 
      {% if setting("coin.coin_picture") %}
      src="{{setting("coin.coin_picture")}}"
      {% else %}
      src="/bundles/topxiaweb/img/order-pay/iconfont-coin.png" 
      {% endif %}
      width="25px">
    </i>
    <h4 class="title">{{setting("coin.coin_name")}}</h4>
    <span>（1元 = {{cashRate}}{{setting("coin.coin_name")}}）</span>
    <div class="price_r">
      <span class="price_r_mark">-</span>
      <span class="pricehead">{% if coursePriceShowType == "RMB" %}￥{% endif %}</span>
      <span class="price_r_num" role="cash-discount"></span>
    </div>
  </div>
  <div class="order-item-detail">
    {% if hasPayPassword %}
    <div class="coin-detail">
      账户余额：<span role="accountCash">{{account.cash|default(0)}}</span>，
      本次使用
      <input role="coinNum" type="text" name="coinPayAmount" value="{{coinPayAmount|default(0)}}" maxlength="">{{setting("coin.coin_name")}}
    </div>
    {% endif %}
    <div class="pay-password">
      {% if not hasPayPassword %}
      <div>
        为了保障账户资金安全，暂时不可用，请先<a href="{{path('settings_pay_password')}}" class="btn-link">设置支付密码</a>。
      </div>
      {% else %}
      <label class="control-label" for="payPassword" style="display:none">支付密码</label>
      <div class="controls">
        <input id="payPassword" name="payPassword" type="password" placeholder="请输入支付密码" data-url="{{path('pay_password_check')}}"/>
      </div>
      <div>
        <a href="{{path('settings_find_pay_password')}}" class="btn-link">忘记密码？</a>
      </div>
      {% endif %}
    </div>
  </div>
</div>