define(function (require, exports, module) {
    var Backbone = require('backbone');

    var Excel = Backbone.Model.extend({
        api: '/excel/checker/',

        initialize : function(type) {
               
        }
    });
});
