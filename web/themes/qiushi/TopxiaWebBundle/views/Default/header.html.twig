<div class="navbar site-navbar one-nav" data-counter-url="{{ path('user_remind_counter') }}">
  <!-- Brand and toggle get grouped for better mobile display -->
  
  <div class="container">
    <div class="container-gap">
      <div class="navbar-header clearfix">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse2">

  <span class="fa fa-user fa-stack-1x"></span>
        </button>
        <button type="button" class="navbar-toggle collapsed pull-left" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="fa fa-navicon"></span>
        </button> 
                  {% if setting('site.logo') %}
            <a class="navbar-brand-logo" href="{{ path('homepage') }}"><img src="{{ asset(setting('site.logo')) }}"></a>
          {% else %}
            <a class="navbar-brand" href="{{ path('homepage') }}">{{ setting('site.name', 'EDUSOHO') }}</a>
          {% endif %}

		  {% set firstCategories = data('Categories',{ group:'course'}) %}

		  
		 <div class="nav navbar-nav hidden-sm hidden-xs">
          <div class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
               ><span class="fa fa-navicon"></span>课程快速导航菜单</a>
            <ul class="dropdown-menu course-expoler" role="menu" style="padding: 5px 10px;">
                {% for firstCat in firstCategories %}
					{% if firstCat.parentId == 0 %}
						<li><a class="list" href="{{ path('course_explore', {category:firstCat.code|default(firstCat.id),first:firstCat.id}) }}">{{ firstCat.name }}</a>
							<ul class="sec-list">
								{% set secCategories = data('Categories',{ group:'course',parentId:firstCat.id}) %}
									{% for secCat in secCategories %}
									<li><a href="{{ path('course_explore', {category:secCat.code|default(secCat.id),first:firstCat.id}) }}">{{ secCat.name }}</a></li>
									{% endfor %}
							</ul>
						</li>
            		{% endif %}
				{% endfor %}             
            </ul>
          </div>
		 </div>
        
        <form class="navbar-form navbar-left hidden-sm hidden-xs clearfix" role="search" action="/search">
          <div class="form-group">
            <input type="search" class="form-control" placeholder="搜索课程" name="q">
            <button type="submit" class="btn"><span class="fa fa-search"></span></button>
          </div>
        </form>
	
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      
      <ul class="nav navbar-nav navbar-right user-menu  hidden-xs">
<li><a href="tel:4008006219"><i class="fa fa-phone" style="margin-right:5px;"></i>400-800-6219</a></li>
      	{% if app.user %}
	        <li><a href="{{ path('my') }}">我的课程</a></li>
	        <li class="dropdown">
	          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><img
	            class="img-circle" src="{{ file_path(app.user.smallAvatar, 'avatar.png') }}" alt=""/></a>
	          <ul class="dropdown-menu text-center" role="menu">
              <li><a href="{{ path('user_show', {id:app.user.id}) }}">个人主页</a></li>
	            <li><a href="{{ path('settings') }}">个人中心</a></li>
				<li><a href="{{ path('my_bill') }}">账户中心</a></li>
	            <li><a href="{{ path('message') }}">消息</a></li>
	            <li><a href="{{ path('notification') }}">通知</a></li>
	            
	            
	            {% if is_granted('ROLE_BACKEND') %}
                    <li><a href="{{ path('admin') }}">管理后台</a></li>
                   
                {% endif %}
	            <li><a href="{{ path('logout') }}">退出</a></li>
	          </ul>
	        </li>
	    {% else %}
            <li><a href="{{ path('login', {goto:_target_path|default(app.request.server.get('REQUEST_URI'))}) }}">登录</a></li>
            <li><a href="{{ path('register', {goto:_target_path|default(app.request.server.get('REQUEST_URI'))}) }}">注册</a></li>
        {% endif %}
      </ul>
      <!-- /.navbar-collapse -->
    </div>
  </div>
  <div class="menu-sec" style="overflow: hidden;">
    <div class="container">
      <div class="collapse navbar-collapse">
        {{ render(controller('TopxiaWebBundle:Default:topNavigation', {siteNav:siteNav|default(null)})) }}
              <ul class="nav navbar-nav navbar-right hidden-xs" sytle="font-size: 14px;">
        {% if setting('mobile.enabled') %}
          <li>
            <a style="font-size: 14px" href="{{ path('mobile') }}" class=""><span class="fa fa-mobile hidden-sm hidden-xs"></span>手机版</a>
          </li>
        {% endif %}

          <li>
            <a style="font-size: 14px" href="{{ path('guide') }}" class="">新手向导</a>
          </li>

          <li>
            <a style="font-size: 14px" href="javascript:void(0);" onclick="AddFavorite('{{ setting("site.name", "EDUSOHO") }}',location.href)" class=""><span class="fa fa-heart hidden-sm hidden-xs"></span>收藏本站</a>
          </li>
        </ul>

      </div>
      <div class="collapse navbar-collapse2 hidden-md hidden-lg hidden-sm">
      	<ul class="nav navbar-nav">
      	{% if app.user %}
	            <li><a href="{{ path('settings') }}">个人中心</a></li>

	            <li><a href="{{ path('message') }}">消息</a></li>
	            <li><a href="{{ path('notification') }}">通知</a></li>
	            	            {% if is_granted('ROLE_BACKEND') %}
                    <li><a href="{{ path('admin') }}">管理后台</a></li>
                   
                {% endif %}
	            <li><a href="{{ path('logout') }}">退出</a></li>
	          </ul>
		 {% else %}
            <li><a href="{{ path('login', {goto:_target_path|default(app.request.server.get('REQUEST_URI'))}) }}">登录</a></li>
            <li><a href="{{ path('register', {goto:_target_path|default(app.request.server.get('REQUEST_URI'))}) }}">注册</a></li>
        {% endif %}	  
      </div>
    </div>
  </div>
  <!-- /.container -->
  <!--主题顶部导航条结束-->
</div>