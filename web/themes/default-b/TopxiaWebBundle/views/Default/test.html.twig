{% import "TopxiaWebBundle::macro.html.twig" as web_macro %}

{% for review in data('LatestCourseThreadsByType',{count:'8', type:'discussion' }) %}
      {% if review %}
      {{ review.courseTitle }}
     {#  {% set poster = review.latestPostUserId %}
      	{{ web_macro.user_link(poster, 'link-muted') }} #}
      	-----------
      {% endif %}
{% endfor %}