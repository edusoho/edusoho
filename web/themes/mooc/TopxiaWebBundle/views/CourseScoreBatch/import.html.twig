{% extends 'TopxiaWebBundle:CourseManage:layout.html.twig' %}

{% block title %}导入管理 - {{ parent() }}{% endblock %}
{% set side_nav = 'transcripts' %}
{% block main %}
  {% if course.status == 'published' %}
    {{ render(controller('CustomWebBundle:CourseScoreBatch:ValidateExcelInfo',{request:app.request, courseId:course.id})) }}
  {% else %}
  {% endif %}
{% endblock %}